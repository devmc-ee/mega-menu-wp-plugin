import { MenuMetaAction, MenuMetaState } from "./menuMeta.type";

export const menuMetaReducer = (state: MenuMetaState, action: MenuMetaAction): MenuMetaState => {
  switch (action.type) {
    default:
      return state;
  }
}